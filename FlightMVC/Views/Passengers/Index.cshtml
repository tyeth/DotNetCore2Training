@model IEnumerable<FlightMVC.Models.PassengerDetails>

@{
    ViewData["Title"] = "Index";
    int counter = 0;
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    <label asp-for="@Model.First().Name"></label>
                    @*@Html.DisplayNameFor(model => model.Name)*@
                </th>
                <th>
                    @*@Html.DisplayNameFor(model => model.Weight)*@
                    <label asp-for="@Model.First().Weight"></label>
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
    
        <tr>
            <td>
                <p asp-for="@item.Name">@(++counter).</p>
                @*@Html.DisplayFor(modelItem => item.Name)*@
            </td>
            <td>
                <p asp-for="@item.Weight"></p>
                @*@Html.DisplayFor(modelItem => item.Weight)*@
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.Name">asp-for DETAILS</a> |
                <a asp-action="Edit" asp-route-id="@item.Name">edit</a> |
                <a asp-action="Delete" asp-route-id="@item.Name">delete</a> |
                @*@Html.ActionLink("Edit", "Edit", new {  id=item.Name }) |
        @Html.ActionLink("Details", "Details", new {  id=item.Name }) |
        @Html.ActionLink("Delete", "Delete", new {    id=item.Name })*@
            </td>
        </tr>
}
    </tbody>
</table>
